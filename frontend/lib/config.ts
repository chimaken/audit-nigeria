export const DEFAULT_ELECTION_ID = Number(
  process.env.NEXT_PUBLIC_DEFAULT_ELECTION_ID ?? 1,
);

/** Lagos — used for Lagos-only demo affordances (e.g. senate grouping, Ikeja shortcut). */
export const DEFAULT_STATE_ID = 1;
